# full-002: direct exploratory recursive trajectory

Frozen before any review outcome. The user requested prioritizing the actual recursive experiment over screening. We therefore select the existing case-audit review directly, without the planned nine diagnostics or blinded classification. No diagnostic response has been generated; no screening constitution is imported.

The first unchanged result was a valid, normally completed endorsement, not a truncation or tool failure. A reasonable working hypothesis is that concrete application to difficult cases encourages more discriminating review than broad appraisal of a polished essay. The selected prompt asks about autonomy and risk, confidential evidence and affected parties, and reassurance under uncertainty. It prescribes no answers and explicitly allows the model to endorse the document unchanged. This is a motivated exploratory choice, not evidence that the prompt increases substantive editing frequency.

`configs/full-002.json` freezes original Qwen3.5-9B M0, original 1,059-word C0, the case-audit instruction block, the actual full-information wrapper, and fresh initial review seed 30401. Each subsequent review uses the current trained checkpoint and constitution in a fresh conversation with the same procedure and seed. The complete document remains editable. Explicit unchanged submission ends this trajectory; edited submissions trigger the actual training pipeline. Maximum five completed training rounds.

The fixed Qwen3.5-27B teacher, 1,500 training prompts, full-parameter DPO objective and optimizer configuration remain as documented for stage 2. Each round uses current-student neutral responses and the current pre-update reference, then post-DPO generation of 512 reflections and 64 four-turn interactions, followed by full-parameter SFT. Updated weights carry forward; optimizer states reset between stages. No editing transcript is used for training.

Changes from full-001: case-audit review instructions; model-facing recipe clarifies exact retained-example policy; reflection output allowance rises from 768 to 1,536 tokens while interaction outputs remain 768; explicit half-example retention floors catch gross generation failure; held-out response allowance rises from 2,048 to 8,192 and judge input allowance from 8,192 to 16,384 to reduce censoring. These changes are frozen together for this exploratory trajectory. It does not isolate any one causal effect. Evaluation uses the same 120 held-out prompts, neutral context, fixed judge and rubric, with raw outputs retained.

Execution uses one H200. The former $200 ceiling was explicitly removed. Track spending and avoid idle resources; a renewable orphan-protection deadline is operational protection, not a scientific stopping rule. Host driver forward compatibility is an engineering accommodation and is recorded separately.

After each completed round, inspect submission/diff, normalized word distance, public reasons, training completion/losses, generation retention and held-out responses. Obtain focused independent feedback while execution continues. Do not wait for extensive classification, change the protocol to force more edits, or alter training based on held-out scores. Label any substantive later protocol change as a new run and preserve this result.
